{
  "name": "bulwark",
  "version": "0.1.0",
  "private": true,
  "description": "Capability-based provenance and behavioural detection for AI agent tool traffic, with minimal active interference.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "eval": "node eval/run.js",
    "eval:sweep": "node eval/sweep.js",
    "demo": "node demo/server.js"
  }
}
