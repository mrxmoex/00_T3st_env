{
  "name": "00_T3st_env",
  "install": "npm ci && npm --prefix web ci",
  "terminals": [
    {
      "name": "dev-server",
      "command": "npm --prefix web run dev -- --hostname 0.0.0.0 --port 3000",
      "description": "Runs the Nahrungsmatrix Next.js app on port 3000."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
