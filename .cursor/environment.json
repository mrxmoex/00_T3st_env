{
  "name": "00_T3st_env",
  "install": "npm ci && npm ci --prefix web",
  "terminals": [
    {
      "name": "dev-server",
      "command": "npm run dev",
      "description": "Runs Du bist was du isst (Vite) on port 3000 with auto-reload."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
