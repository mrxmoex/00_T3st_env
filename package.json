{
  "name": "00_t3st_env",
  "version": "1.0.0",
  "description": "Du bist was du isst — biochemical nutrition evaluation + Notes demo environment.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build:nutrition:lib": "tsc -p nutrition/tsconfig.json",
    "build:nutrition:web": "cd nutrition/web && npm install && npm run build",
    "build:nutrition": "npm run build:nutrition:lib && npm run build:nutrition:web",
    "test": "npm run build:nutrition:lib && node --test",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "eslint": "^9.18.0",
    "typescript": "^5.7.0"
  }
}
