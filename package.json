{
  "name": "00_t3st_env",
  "version": "1.0.0",
  "description": "A small Notes web app used to demonstrate the Cloud Agent development environment end to end.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "npm run preview --prefix web",
    "dev": "npm run dev --prefix web",
    "test": "node --test && npm test --prefix web",
    "lint": "eslint . && npm run lint --prefix web",
    "build": "npm run build --prefix web"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "eslint": "^9.18.0"
  }
}
